#include "main.h"

/**
 * add - function that adds two integers and returns the result
 * @a = first integer to be added to b
 * @b = second integer to be added to a
 * Return: sum of a and b
 */
int add(int a, int b)
{
int result;

result = a + b;
return (result);
}
