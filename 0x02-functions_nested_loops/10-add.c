#include "main.h"

/**
 * add - function that adds two integers and returns the result
 * @a = first integer, @b = second integer
 * Return: 0 if success
 */
int add(int a, int b)
{
int result;

result = a + b;
return (result);
}
